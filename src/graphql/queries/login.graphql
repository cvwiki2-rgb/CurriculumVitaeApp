query Login($auth: AuthInput!) {
  login(auth: $auth) {
    user {
      id
      email
      profile {
        id
        full_name
        avatar
      }
      role
      is_verified
    }
    access_token
    refresh_token
  }
}
